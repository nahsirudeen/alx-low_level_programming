#include <stdio.h>
/**
 * main - Entry points
 * Return: By default returns zero if no error (success)
 */
int main(void)
{
	puts("\"Programming is like a building a multilingual puzzle");
	return (0);
}
